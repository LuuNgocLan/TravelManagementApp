<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="luungoclan.min.traveltourmanagement">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="false"
        android:icon="@mipmap/ic_launcher_foreground"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".views.main.MainActivity"
            android:theme="@style/AppThemeFullScreen" />
        <activity
            android:name=".views.splash.SplashActivity"
            android:theme="@style/AppThemeFullScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".views.login.LoginActivity"
            android:theme="@style/AppThemeFullScreen" />
        <activity
            android:name=".views.detailTour.DetailTourActivity"
            android:theme="@style/AppThemeFullScreen" />
        <activity
            android:name=".views.myProfile.EditProfileActivity"
            android:theme="@style/AppThemeFullScreen" />
        <activity android:name=".views.intro.IntroActivity" />
        <activity android:name=".views.places.ListTourInPlaceActivity" />
        <activity android:name=".views.changePassword.ChangePasswordActivity" />
        <activity android:name=".views.booking.BookingActivity" />
        <activity android:name=".views.tours.AllTourActivity" />
    </application>

</manifest>